import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#66ffff',
    },
    secondary: {
      main: '#aaff00',
    },
  },
});

export default theme